<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.maxim.vesdecod.donations">

  <application
      android:allowBackup="true"
      android:icon="@mipmap/ic_launcher"
      android:label="@string/app_name"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:supportsRtl="true"
      android:theme="@style/AppTheme">
    <activity android:name=".ui.activity.MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>
    <activity android:name=".ui.activity.FundraisingTypeActivity"
        android:parentActivityName=".ui.activity.MainActivity"/>
    <activity android:name=".ui.activity.TargetedFundraisingActivity"
        android:parentActivityName=".ui.activity.FundraisingTypeActivity"/>
    <activity android:name=".ui.activity.RegularFundraisingActivity"
        android:parentActivityName=".ui.activity.FundraisingTypeActivity"/>
    <activity android:name=".ui.activity.TargetedFundraisingExtrasActivity"
        android:parentActivityName=".ui.activity.TargetedFundraisingActivity"/>
  </application>

</manifest>